/**
 * data.example.js
 * Template untuk src/user/data.js
 * Salin file ini: cp src/user/data.example.js src/user/data.js
 * Lalu isi dengan nilai yang sudah dienkripsi via VITE_SECRET_KEY
 *
 * Cara encrypt nilai:
 *   import { encryptValue } from '@/services/crypto'
 *   encryptValue('sheet_id_asli')  → salin hasilnya ke sini
 */

export const users = {
  // 'email@gmail.com': {
  //   sheetId: 'ENCRYPTED_VALUE',
  //   webhook: 'ENCRYPTED_VALUE'
  // }
}
